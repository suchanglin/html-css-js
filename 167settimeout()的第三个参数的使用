setTimeout(func, delay, param1, param2, ...)后面的param1等都作第一个函数func的参数传进去。

setTimeout( function( a, b ,c ) {
    console.log( a );//1
    console.log( b );//2
    console.log( c );//3
}, 1000, 1, 2, 3 );
